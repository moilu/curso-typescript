(()=>{

    //Resúmen: Nunca usar var, a veces usar let pero de preferencia usar const
    const name = 'Moises';

    const getName = (): void => {
        console.log(name);
    }

    // getName();

})();